services:
  - type: web
    name: swisstopo-mcp
    runtime: python
    buildCommand: pip install -e .
    startCommand: python -m swisstopo_mcp.server
    envVars:
      - key: MCP_TRANSPORT
        value: streamable_http
      - key: PORT
        value: 8000
    healthCheckPath: /health
